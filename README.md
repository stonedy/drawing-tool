# Stroke Replay

A browser-based experimental drawing tool where every stroke can be replayed, reversed, looped, hidden, timed, saved, restored, and exported.

Open `index.html` in a browser to run the app.
